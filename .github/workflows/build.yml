name: Build GeoCheats Mod

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Code
      uses: actions/upload-artifact@e3f369dde6c7bb8b7c6c86b5edc2e3c9d7b5f50a


    - name: 🛠️ Install Dependencies
      run: |
        sudo apt update
        sudo apt install -y cmake g++ git

    - name: ⚙️ Configure & Build
      run: |
        cmake -S . -B build
        cmake --build build

    - name: 📦 Upload .geode Mod
      uses: actions/upload-artifact@e3f369dde6c7bb8b7c6c86b5edc2e3c9d7b5f50a
      with:
        name: GeoCheats.geode
        path: build/dist/*.geode
